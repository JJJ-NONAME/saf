unstable_python:
  packageManager: "uv"
  version: "3.11.10"

unstable_uv:
  version: "0.11.11"
  syncArgs:
    - "--inexact" # preserves packages installed outside uv.lock, including the private dependency.
